apiVersion: v1
kind: Service
metadata:
  name: walletpulse-backend-service
  labels:
    app: walletpulse
    component: backend
spec: 
  type: ClusterIP
  selector:
    app: walletpulse-backend
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
